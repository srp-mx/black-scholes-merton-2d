### Uso

Para armar la imagen de Docker
```bash
./build
```

Para correr el contenedor
```bash
./run
```

Si quieres crear un túnel de SSH para conectarte hacia otra máquina remota
```bash
./tunnel-open <usuario@host>
```
